#!/bin/bash
# Requirements: Ensure Kaggle API is set up with `kaggle.json` for authentication, the `data` directory exists for SQLite storage,
# and necessary libraries (`pandas`, `kaggle`, `sqlalchemy`) are installed. Make sure the Kaggle datasets are accessible.

python project/pipeline.py